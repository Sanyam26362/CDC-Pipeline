# Required for logical replication (CDC)
wal_level = logical

# How many replication slots can exist
max_replication_slots = 5

# How many replication connections allowed
max_wal_senders = 5

# Standard settings
listen_addresses = '*'
max_connections = 100
shared_buffers = 128MB